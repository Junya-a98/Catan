# Catan風ゲーム

Pygame で実装した、ローカル対戦と信頼できるLAN内対戦に対応するカタン風ゲームです。将来のWeb公開に向け、同じ権威サーバーで動くローカル専用ブラウザ版も利用できます。
現在は 2〜4 人プレイ、性格を選べる AI、初期配置、通常ターン、盗賊、銀行交易、プレイヤー間交渉、発展カード、最長交易路 / 最大騎士力まで遊べます。公式基本ゲームに準拠する人数は 3〜4 人で、2 人プレイは簡易バリアントです。

今回の改善で、次の点を優先して強化しています。

- ダイス演出を内部ロジックと同じ `2d6` 表現に統一
- 「今やるべきこと」と「なぜできないか」を UI 上で分かりやすく表示
- 盤面生成に `mode` と `seed` を導入し、再現性と初期盤面バランスを改善
- 開始前 UI から `mode` / `seed` を切り替えて盤面を再生成できるように改善
- 開始前 UI で勝利点を 5〜15 点から選択可能にし、設定をセーブへ保持
- 0〜人数-1人の簡単なAIプレイヤーを追加
- 資源銀行を各19枚にし、配布不足・支払い・捨て札・港交易を公式ルールに合わせて修正
- 街道を木製コマ、開拓地と都市を立体的な建物、盗賊をポーン、港を桟橋と看板として描画
- 港の桟橋を海側へ分離し、港付きの辺に置いた街道が隠れないよう改善
- 港ラベルが右パネルに隠れないよう表示範囲を補正
- プレイヤーごとの記号と駒パターンを加え、色だけに頼らず所有者を判別可能に改善
- 相手の資源内訳と勝利点カードを非公開化
- 人間同士の手番・初期配置・捨て札・交渉の切り替わりに手札を隠す交代画面を追加
- 盤面上部に手番・現在ステップ・次の操作を固定表示
- 左ログは通常折りたたみ、必要な時だけ開いてゲーム開始時まで遡れるよう改善
- AI速度を変更・一時停止でき、判断理由と公開された資源獲得を追いやすく改善
- AIの建設候補、交易、発展カード、盗賊、最大騎士力の評価を強化
- AIの交渉相手を、公開された直近獲得と盤面の生産力から推測して選択
- 国内交易で個別交渉に加えて「全員に募集」を選べるようにし、最初の承諾者と成立
- 対局をイベント単位で自動記録し、終了後や次回起動時に見返せるリプレイビューアーを追加
- 対局後に最終順位・VP推移・累積建設・交易・運指数・重要イベントをまとめるリザルト画面を追加
- リザルトの重要イベントから、対応するリプレイ位置へ直接移動可能
- 同じルール処理でAI戦を反復し、勝率・初期配置・出目を分析する自己対戦ダッシュボードを追加
- 自己対戦をプロセス並列化し、数百〜数千戦の分析を短時間で実行可能に改善
- LAN対戦の権威サーバーとPygameクライアントを追加し、部屋作成・参加コード・再接続・観戦・二重操作防止を実装
- ルール処理と非公開情報マスキングを共用する、依存追加なしのローカル専用ブラウザ版を追加
- ブラウザ盤面を同梱の生成地形・海面、確率ドット、立体的な街道・建物・盗賊・桟橋付き港へ刷新し、外部画像通信なしでローカル版の雰囲気へ統一
- ブラウザの実際の `2d6` 出目を盤面中央で再現し、街道・開拓地・都市の完成時に一度だけ表示する建設アニメーションを追加
- ブラウザへダイス・建設・交易・勝利の効果音と、任意で再生できるオリジナルBGM、音響ON/OFF設定を追加
- ブラウザの部屋作成時にAI人数と標準・混合・拡大・交渉・妨害の性格を選択可能にし、AI判断を段階的な盤面更新とコメントで表示
- ブラウザへ最終順位・VP推移・建設・交易・運指数のリザルトと、重要イベントから移動できる閲覧者別リプレイを追加
- ブラウザ通信をWebSocket優先にし、接続できない場合は既存のsame-origin HTTP pollingへ自動でフォールバック
- 開始前の詳細設定に、標準19タイルのカスタムマップ編集とハウスルール切り替えを追加
- カスタム設定をセーブ・リプレイ・LANへ同じversion付きdocumentで引き継ぎ、fingerprintで改ざんや取り違えを検出
- AIを標準・拡大重視・交渉重視・妨害重視から選べるようにし、自己対戦では席を自動ローテーション
- 街道建設カードは、合法な配置先がある限り公式どおり2本を連続配置
- 無効ボタンを明確に減光し、残りコマを省略のない4つの情報チップで表示
- セーブ状態を再利用したネットワーク同期プロトコルと、プレイヤー別の非公開情報マスキングを追加
- テストと README を現状仕様に追従

## 起動方法

### 必要環境

- Python 3.10 以上
- `pygame`

### セットアップ

```bash
python3 -m venv .venv
. .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install -r requirements-dev.txt
```

`Makefile` を使う場合:

```bash
make venv
make install-dev
```

### ゲーム開始

```bash
PYTHONPATH=python python3 python/main.py
```

セットアップ済みなら `make run` でも起動できます。

### ローカルブラウザ版

将来のWeb対戦へ向けたローカル実行版です。追加パッケージは不要です。

```bash
make web
```

起動後に [http://127.0.0.1:8765/](http://127.0.0.1:8765/) を開きます。部屋作成、6文字コードでの参加、ready、対局開始、再接続、観戦、サーバーが提示した合法手の操作に対応しています。部屋作成時には総人数の範囲内でAI席を追加し、標準・混合・拡大重視・交渉重視・妨害重視から性格を選べます。AI席は最初からreadyで、サーバーが判断を1段階ずつ進めるため、盤面更新とコメントから行動を追えます。

ダイスは権威サーバーが公開した実際の2個の目でアニメーションし、街道・開拓地・都市は新しく置かれた駒だけを強調します。画面上部で効果音とBGMを個別に切り替えられます。BGMは既定でOFFで、ブラウザの自動再生制限に従い、最初の操作より前には音声処理を開始しません。音は外部ファイルを取得せず、このプロジェクト用に作成した音色と短い曲をWeb Audio APIで端末内合成します。

対局終了後は、最終順位、VP推移、累積建設数、国内・銀行交易数、運指数、重要イベントを表示します。重要イベントやスライダーから、認証済みの自分用に非公開情報をマスクしたサーバー側リプレイへ移動でき、最初・前・再生 / 一時停止・次・最後・再生速度を操作できます。観戦者のリプレイでは全員の手札内訳を伏せます。

通信はsame-origin WebSocketを優先し、利用できない場合や切断時は既存のJSON APIとHTTP pollingへ自動で切り替わります。複数席を1台で試す場合、cookieを共有しない別ブラウザまたは別プロファイルを使ってください。

この段階では安全のためloopbackにだけ接続し、InternetやLANの別端末には公開しません。実装済みのWebSocketも平文のローカル接続用です。公開運用にはTLS / WSS終端、account認証、永続session、rate limitなどを追加する必要があります。設計とAPIは [`docs/web-mvp.md`](docs/web-mvp.md) を参照してください。

### AI自己対戦と統計ダッシュボード

通常ゲームと同じルール・AIを、画面表示、音声、待ち時間、リプレイ保存なしで20戦実行します。

```bash
make simulate
```

完了すると `self-play-reports/self-play-latest.json` と、外部通信を行わないローカルHTMLダッシュボードが生成されます。試合数やseedを指定する例:

```bash
PYTHONPATH=python python python/simulate.py --games 100 --seed 42
```

数百〜数千戦をCPU数に合わせて並列実行する例:

```bash
# 0は使用するworker数をCPU数と試合数から自動決定
PYTHONPATH=python python python/simulate.py --games 500 --seed 42 --workers 0

# worker数を固定し、試合ごとの進捗表示を省略
PYTHONPATH=python python python/simulate.py --games 1000 --seed 42 --workers 8 --quiet
```

特定の盤面だけを繰り返し検証する場合:

```bash
PYTHONPATH=python python python/simulate.py \
  --games 100 --board-seed 86712347 --mode constrained
```

4人戦では、指定を省略すると `standard`（標準）、`expansion`（拡大重視）、
`trader`（交渉重視）、`disruptor`（妨害重視）を1人ずつ割り当てます。
性格と席の有利不利を混同しないよう、試合ごとに割り当てる席を1つずつ回します。
構成を固定したい場合は席1から順に指定できます。同じ性格を複数指定するミラー戦も可能です。
以前の全員標準AIによるseed結果を再現するときは、4人戦なら
`--personalities standard,standard,standard,standard` を明示してください。

```bash
PYTHONPATH=python python python/simulate.py --games 100 \
  --personalities expansion,trader,disruptor,standard
```

主なオプション:

- `--games`: 試合数（既定20）
- `--seed`: ダイス・発展カード・略奪を再現する連番seedの開始値
- `--board-seed`: 全試合で固定する盤面seed。省略時は試合ごとに変更
- `--mode`: `constrained` / `fully_random`
- `--players`: 2〜4人（公式基本ゲーム準拠の分析は3〜4人を推奨）
- `--personalities`: 席順のAI性格をカンマ区切りで指定（人数と同数が必要）
- `--target`: 勝利点5〜15
- `--workers`: `0` は自動、`1` は再現性を確認しやすい逐次実行、`2`〜`32` は指定数のプロセスで並列実行（既定 `0`）
- `--open`: 生成後にHTMLを既定ブラウザで開く

各試合にはターン・終了理由の上限を設け、停止した試合を勝率へ混ぜません。さらに資源カード19枚、各プレイヤーの駒数、街道の辺重複、勝者VP、ダイス回数を対局後に検査します。ダッシュボードでは性格別・性格×席の勝率に加え、国内交易の提案・成立、銀行交易、盗賊移動、騎士使用、最終的な街道・開拓地・都市数の1試合平均も比較できます。

AI相性は、性格ごとの全体勝率だけでなく「どの性格を相手にしたとき勝ちやすいか」を相性表で確認します。席順や初手番の偏りも併記されるため、出場数が少ない行や警告付きの数値は結論として扱わず、試合数を増やして比較してください。盤面公平性は `--board-seed` で同じ盤面を固定し、match seedだけを変えて席別勝率・信頼区間を読み、別表の初期配置統計と合わせて判断します。複数の盤面を比較するときは `--board-seed` を変えて同じコマンドを反復します。今回は `--board-count` のような追加APIを設けず、1回のbatchが検証する盤面を明確に保ちます。詳しい結果スキーマは [`docs/self-play-report.md`](docs/self-play-report.md) にあります。

将来のWeb版では、CLIや画面描画に依存しない `game.self_play.run_batch` APIを常駐サーバーのjob queueから呼び出す方針です。HTTPリクエスト内で数千戦を待たせず、job IDで進捗を取得し、完成したversion付きJSON / HTMLを後から参照できる境界にします。

開始前の右パネルで、次の設定をそのまま変更できます。

- プレイヤー人数
- AI プレイヤー人数
  - `0` から `プレイヤー人数 - 1` までボタンで切り替え
- AI 性格
  - 性格ボタンで `標準`（既定）/ `混合` / `拡大重視` / `交渉重視` / `妨害重視` を選択
  - `混合` は CPU ごとに `拡大重視` / `交渉重視` / `妨害重視` を順番に割り当て
- 勝利点
  - `5` から `15` 点まで増減（既定は `10` 点）
- 盤面生成 `mode`
  - `制約付き` (`constrained`)
  - `公式ランダム` (`fully_random`)
  - 詳細設定で編集した `カスタム` (`custom`)
- 詳細設定
  - 地形（数字チップごと）・数字チップ・港を2つ選んで入れ替え
  - 地形・数字・港を個別にシャッフル
  - 資源18枚＋砂漠1枚、公式数字チップ18枚、港9か所の構成は常に維持
  - 6/8隣接などの偏りは適用前に警告
  - `キャンセル` では盤面を変更せず、`適用` 時にまとめて反映
- ハウスルール
  - 港がない銀行交易を `3:1` に変更（資源港の `2:1` は維持）
  - `7` の出目で手札を捨てず、盗賊移動から開始
  - 発展カードを種類ごとに山札から除外
  - 既定はすべて無効で、従来どおりのルール
- `seed`
  - 直接入力して `seed反映`
  - `再生成` で新しい盤面を作成
- 開始前サマリー
  - 現在の mode / seed
  - mode の簡易説明
  - seed 入力中の操作ガイド

生成盤面は同じ `mode + seed`、カスタム盤面は同じmap documentなら再現できます。
設定は最初の初期ダイスを振るまで変更でき、開始後は対局とリプレイの整合性を保つため固定されます（AI速度は変更可能です）。
カスタム設定のdocument形式とWeb版での再利用方針は [`docs/custom-settings.md`](docs/custom-settings.md) を参照してください。

## いま遊べる内容

- 初期ダイスで配置順を決定
  - 最高点の同点だけ自動で再ロール
  - 開始プレイヤー以降は席順を維持
- 初期配置 2 ラウンド
  - 2 ラウンド目の開拓地で初期資源を獲得
- 通常ターン
  - `2d6` ダイス演出
  - 出目に応じた資源配布
  - `7` で盗賊フェーズ
- 建設
  - 街道 / 開拓地 / 都市
- 発展カード
  - 騎士 / 街道建設 / 収穫 / 独占 / 勝利点
- 銀行交易
  - 港の交易比率にも対応
- 有限の資源銀行
  - 各資源19枚
  - 複数プレイヤーへの配布不足時は該当資源を配布しない
  - 1人だけが対象なら銀行に残っている枚数まで配布
- AIプレイヤー
  - 初期配置 / ダイス / 捨て札 / 盗賊 / 建設 / 発展カード / 銀行交易 / 国内交易を自動処理
  - 序盤は資源の多様性、後半は麦・鉄と都市を重視
  - 公開得点の高い相手への盗賊、建設目標を完成させる交易を優先
  - 騎士カードは最大騎士力や盗賊移動の必要がある時まで温存
- 国内交易・交渉
  - ダイス解決後、建設などと好きな順番で何度でも実行可能
  - 手番プレイヤーから任意の相手へ複数資源・複数枚を提案
  - 「全員に募集」では同じ条件を席順に提示し、拒否なら次の相手へ進行
  - 最初に承諾したプレイヤーと交易が成立し、全員拒否なら不成立
  - 承諾 / 拒否 / 条件変更に対応
  - AIは建設目標と資源価値から自動で回答
  - 人間同士では手札を隠す画面受け渡しを表示
- 勝利条件
  - 開始前に設定した 5〜15 点へ到達すると勝利（既定は 10 点）
- セーブ / ロード
  - `F5` でゲーム全体をクイック保存
  - `F9` で保存地点から再開
  - 交渉・盗賊・捨て札・AI設定・全イベント履歴も復元
  - JSON形式、バージョン検査、資源・駒数検証、失敗時ロールバックに対応
  - カスタムマップ全文・ハウスルール・SHA-256 fingerprintも保存し、旧セーブは標準設定として読み込み
- リプレイビューアー
  - 勝利時に対局を `replays/` へ自動保存
  - 終了画面の「対局をリプレイ」、または次回の初期画面の「直前の対局をリプレイ」から開始
  - イベント単位の前後移動、先頭 / 末尾、自動再生 / 一時停止に対応
  - 閲覧中はAI・ダイス・通常入力を完全停止し、終了すると閲覧前の状態へ復帰
  - 通常は公開情報だけを表示し、ローカル分析時だけ `V` で全員の手札を明示公開
  - 過去フレームから対局を再開する機能はなく、閲覧専用
- 対局後リザルト
  - 最終順位、VP推移、街道・開拓地・都市の累積建設数、銀行・国内交易数を表示
  - 運指数は実際に生産できた資源量を盤面上の確率期待値と比較し、`100` を期待どおりとして表示
  - 重要イベントは上下キーまたはクリックで選び、`Enter` / `Space` で該当リプレイ位置へ移動
  - `R` で同じ盤面、`N` で新しい盤面の対局を開始
  - 集計はPygame非依存のversion付きJSONで生成し、将来のWeb版・LANサーバーでも共用可能
  - データ形式は [`docs/match-result.md`](docs/match-result.md) を参照
- カスタムマップ・ハウスルール
  - 初期画面の `詳細設定` から全画面editorを開き、1200x800以上で盤面と操作を重ねず表示
  - custom mapはPygame座標ではなくaxial座標・資源・数字・港slotのJSONとして保持
  - 同じ設定documentをローカル対局、セーブ、リプレイ、LANの権威サーバーで共有
  - 将来のWebクライアントも同じdocumentと公開盤面manifestを利用できる境界
- AI自己対戦・統計ダッシュボード
  - 通常ゲームと同じ `CatanGame` / `SimpleAI` / 公式ルール判定を直接利用
  - `--workers 0` の自動選択、または1〜32 workerで逐次 / プロセス並列実行
  - match seedと盤面seedを分離し、同じ条件を再現可能
  - 完走率、平均・中央値ターン、席順・初手番・手番位置別勝率を集計
  - 初期配置のpip、6・8への接触、資源種類と勝率を比較
  - `2d6` の実測分布と期待分布の差を表示
  - 全体100戦未満、または比較行20出場未満は小標本として注意を表示
  - JSONと、JavaScript・CDN・外部フォント・アクセス解析を使わないHTMLを原子的に保存
  - 将来のWeb版は常駐job queueから同じ集計APIを呼び、進捗と成果物を非同期配信

## UI の見方

- 盤面上部の進行ヘッダー
  - 「あなたの手番」または「AI進行中」をプレイヤー色で表示
  - `ダイス → 行動 → 終了` の現在位置を常時表示
  - 盗賊や捨て札中は、その特殊処理の指示へ切り替え
- 右パネル
  - 最上部の「今やること」に必要な操作を固定表示
  - 建設ボタンに必要資源を併記
  - 無効な行動は押せない状態、手番終了などの主要操作は強調表示
  - 手札・街道・開拓地・都市の残数を、完全な名称のチップで表示
  - 建設プレビュー = 資源不足 / 在庫切れ / 建設可能 を表示
  - 銀行に残っている各資源の枚数を表示
  - ボタンが多い場面では建設プレビューを2列に圧縮し、下端の見切れを防止
  - 開始前は盤面 `mode` と `seed` をここで調整
- 盤面ハイライト
  - 光っている場所だけが現在の有効候補
  - 盗賊移動、略奪対象、建設候補も状態に応じて強調
- 左ログ
  - `L` または左上のバーで開閉
  - マウスホイール、`PageUp / PageDown` で移動
  - `Home` でゲーム開始時、`End` で最新へ移動
  - 直前のダイス・建設・交易・発展カード結果を上部カードに表示
  - 手番が変わってもイベント履歴を保持
  - 無効操作の理由
  - 資源配布や効果解決の履歴
- プレイヤーカード
  - 現在の手番をプレイヤー色の太枠と「手番中」で表示
  - 勝利点を `現在点 / 目標点` で常時表示
  - `あなた` / `人間` / `CPU` を区別
  - `● / ◆ / ▲ / ■` の記号でプレイヤーを色以外でも区別
- 画面上の一時メッセージ
  - クリック直後や無効操作直後に、失敗理由を短く表示
- 数字トークン
  - `6` / `8` は赤で強調
  - 下の pip 数で出やすさを表現

## 操作方法

### 基本操作

- `Space`: ダイスを振る / 初期ダイス
- `Enter`: 手番終了
- `Esc`: 建設や選択をキャンセル
- `H`: 左下のヘルプを開閉
- `L`: イベント履歴を開閉
- `A`: AI速度を変更（ゆっくり / 標準 / 高速 / 一時停止）
- `PageUp / PageDown`: 履歴をページ移動
- `Home / End`: 履歴の最初 / 最新へ移動
- `F5`: クイックセーブ
- `F9`: クイックロード

### リプレイ操作

- `← / →`: 1イベント前 / 次へ
- `Home / End`: 最初 / 最後のイベントへ
- `Space`: 自動再生 / 一時停止
- `V`: 全員の手札を公開 / 非公開
- `H`: リプレイ用ヘルプを開閉
- `Esc`: リプレイを終了して元の画面へ戻る

リプレイJSONは手札を含むローカル解析用データです。発展カード山札の順序は保存時に正規化していますが、ファイル自体にはプレイヤーの非公開情報が含まれるため、そのまま他人へ共有しないでください。

セーブ先は `saves/quicksave.json` です。ダイス演出中だけは、出目の二重解決を防ぐため保存できません。

### 建設・行動

- `R`: 街道
- `S`: 開拓地
- `C`: 都市
- `D`: 発展カード購入
- `T`: 銀行交易
- `P`: プレイヤーとの交渉

### 発展カード

- `K`: 騎士
- `B`: 街道建設
- `Y`: 収穫
- `M`: 独占

### 資源選択系

- `1`: 木
- `2`: 羊
- `3`: 麦
- `4`: 土
- `5`: 鉄

## LAN対戦

LAN / オンライン対戦の前提として、次の権威サーバー基盤を実装しています。

- セーブ状態を基にしたrevision付き同期スナップショット
- 閲覧プレイヤー本人以外の資源内訳・発展カード・勝利点カードを自動で非公開化
- 発展カード山札は順番を伏せ、残り枚数だけを同期
- TCPの分割受信・複数メッセージ結合に対応するlength-prefix JSON frame
- 最大メッセージサイズ、protocol version、操作種別の検証
- 2〜4人の部屋作成、参加コード、ready、host開始、観戦
- 人間を最低1人残したAI席の予約、AI性格設定、ready不要のCPU参加者表示
- AI判断を上限付きの1ステップごとに進め、各判断を個別revisionとして配信
- 再接続tokenによる席の復帰と、revision / sequenceによる二重操作防止
- 待機室の明示退出による即時席解放と、host不在時の接続済みplayerへの権限引き継ぎ
- 対局中の再接続期限切れを全参加者へ通知し、進行不能な部屋を安全に終了
- 対局中の明示退出は全参加者へ通知し、その対局を即時終了
- 接続sessionから席を確定するsemantic command検証
- Webでも再利用できるstable ID付き公開盤面manifest
- 閲覧席ごとに非公開情報を伏せた上限付きサーバーリプレイと、公開リザルトJSON
- 開始前画面から開けるレスポンシブロビーと対局画面
- serverが配布する合法手だけを表示・送信する操作UIと、読み取り専用の観戦UI

開始前画面の `LAN対戦` からロビーを開けます。同じ端末がhostとclientの両方となる部屋作成と、`host:port` を指定する参加・観戦に対応しています。既定のportは `47624` です。

遊び方:

1. ホストは開始前画面で人数・勝利点・盤面を決め、`LAN対戦` → `部屋を作成` を選びます。
2. 参加者は表示された接続先と6文字の参加コードを入力し、`参加する` または `観戦する` を選びます。
3. プレイヤー全員が `準備完了` にした後、ホストが `対局を開始` を押します。
4. 対局中はサーバーが提示した合法なボタンや光る配置先だけを操作できます。`Esc` で接続を保ったままロビー情報へ戻れます。

macOSのファイアウォール確認が出た場合は、同じ信頼できるLAN内で遊ぶときだけPythonへの着信を許可してください。Internetへ `47624` を公開しないでください。

## 盤面生成

開始前 UI に加えて、`GameBoard` と `CatanGame` はコードからも `mode` と `seed` を受け取れます。

- `constrained`
  - デフォルト
  - `6` と `8` の隣接を避ける
  - 高確率トークンの偏りを減らす
- `fully_random`
  - 資源・数字・港をシャッフル
  - 公式ルールに従い `6` / `8` は隣接させない

`seed` を固定すると、同じ資源配置 / 数字配置 / 港配置を再生成できます。

開発用にコードから起動する例:

```python
from game.game import CatanGame

CatanGame(board_mode="fully_random", board_seed=42, ai_player_count=1).run()
```

## 参照した公式ルール

- [CATAN公式ルールブック配布ページ](https://www.catan.com/understand-catan/game-rules)
- [CATAN基本ゲーム公式FAQ](https://www.catan.com/faq/basegame)

主に初期配置順、資源カード19枚制、銀行在庫不足時の配布、盗賊、発展カード、最長交易路、最大騎士力、港交易、国内交易、手札の非公開情報を照合しています。

## テスト

依存は `.venv` など、ゲーム起動と同じ Python 環境へ入れてください。

```bash
python3 -m venv .venv
. .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install -r requirements-dev.txt
```

その上で、次のコマンドを推奨します。

```bash
PYTHONPATH=python python -m pytest tests
```

`Makefile` を使う場合:

```bash
make test
```

`pytest` 単体コマンドを使う場合も、必ず同じ仮想環境を有効化してください。環境が分かれていると `pygame` や `pytest` の片方だけが見つからないことがあります。

CI では GitHub Actions で headless 実行します。

- `SDL_VIDEODRIVER=dummy`
- `SDL_AUDIODRIVER=dummy`
- `PYTHONPATH=python`

ローカルで headless 実行したい場合も同じ環境変数を付ければ CI と近い条件で確認できます。

```bash
SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy PYTHONPATH=python python -m pytest tests
```

主なテスト対象:

- `2d6` ダイス演出と状態遷移
- 建設候補ハイライト
- 無効操作理由とガイダンス文言
- 開始前 UI の盤面 `mode` / `seed` 設定
- 開始前 UI の勝利点設定、勝利判定、旧セーブ互換
- `constrained` 盤面での `6/8` 非隣接
- `seed` 固定時の再現性
- 港交易レート
- 資源銀行の保存則と在庫不足時の配布
- AI の初期配置・建設・ターン進行
- AI速度・一時停止・判断表示と、都市完成を優先する交易戦略
- 標準・拡大・交渉・妨害AIの意思決定差、旧セーブ互換、性格設定UI
- AIが非公開手札を使わず、公開生産力と直近獲得から交渉相手を選ぶこと
- 通常ターン・初期配置・交渉・特殊フェーズを含むセーブ / ロード往復
- 破損セーブ、資源総数改ざん、ダイス演出中保存の拒否
- リプレイの原子的保存、サイズ・版・フレーム検証、乱数とAI待機時刻の副作用防止
- リプレイ中のAI停止、通常入力遮断、前後移動、終了時のライブ状態復元
- リプレイタイムライン、進捗バー、公開情報 / 全手札表示の切り替え
- プレイヤー別の非公開情報を伏せたネットワークスナップショット
- Webでも再利用できるstable ID付き公開盤面manifest
- TCP分割受信・連結受信・巨大frame・未許可操作の検証
- LAN部屋の満席・ready・host開始・観戦・切断席予約・token再接続
- 明示退出時の席解放、host引き継ぎ、対局中の再接続期限切れ終了通知
- 接続session由来の操作権限、revision、sequence、exactly-once実行
- localhostで部屋作成から対局開始・操作・再接続までの通信往復
- ローカルWeb APIのsession・同一origin制約・部屋作成・broadcast・再読込復元・非公開手札
- WebのAI席・性格設定、AIの上限付き自動進行、失敗時rollbackと乱数状態の分離
- WebSocketのhandshake・mask・JSON・サイズ制限・Ping / Pong / Closeと、HTTP fallback
- Webリザルトの順位・VP推移・建設・交易・運指数と、重要イベントから閲覧者別リプレイへの移動
- サーバーリプレイの履歴上限、読み取り専用化、席別手札・提示前交易条件・発展カード山札順の非公開化
- LANロビーの画面遷移・入力・非同期接続と、対局画面のレスポンシブ描画・hit test
- server配布の操作候補だけが有効になり、観戦者は操作できないこと
- 国内交易の個別提案・全員募集・拒否継続・条件変更・AI応答・資源保存則
- ヘッドレス対局が画面・音声・リプレイへ触れないこと、seed再現性、停止上限、乱数状態復元
- 自己対戦後の資源・駒・道路・勝者・ダイス整合性と、統計JSON / HTMLの安全な生成
- 人間プレイヤー交代時の手札非表示と、AIが非公開の手札内訳を参照しないこと
- 街道・開拓地・都市・港の描画
- 港ラベルが街道・建物・数字チップ・進行ヘッダー・他の港と重ならないこと
- 最長交易路 / 盗賊の基本処理
- 街道建設カードの2本連続配置と、残りコマ1本時の例外
- 無効ボタンと残りコマ情報チップの描画

## 未対応・既知の範囲外

今回の対象外:

- Internet公開用の暗号化・account認証・NAT越え

LANバックエンドの構成、API、安全上の範囲は [`docs/lan-networking.md`](docs/lan-networking.md) を参照してください。

今後の実装候補:

- AI性格ごとの強さの継続調整と、対戦相性の信頼区間表示
- 自己対戦jobの永続化・中断・再開と、複数盤面batchの比較UI
- LAN参加コードの自動探索と、サーバー再起動をまたぐsession永続化
- ブラウザ版のカスタムマップ / ハウスルール設定UI
- インターネット公開時のTLS、account認証、永続セッション、rate limit
- 通常ルールから分離した、予告イベント・フロンティア探索・交易2.0の独立バリアント

### 独立バリアントのロードマップ

無処理の `standard` を保存・通信・リプレイへ引き継ぐ共通基盤と、バリアントの公開状態だけをLAN / Web・観戦・ネットワークリプレイへ渡す境界まで実装済みです。完全なセーブにだけ権威サーバー用の非公開状態を残し、公開snapshotでは `private` キー自体を除去します。ゲーム内容が変わる `forecast_events` / `frontier` / `trade2` と開始画面での選択は未実装です。既存のハウスルールへ機能を継ぎ足さず、将来はこれらのいずれか1モードだけを開始時に選ぶ構成とし、`standard` は現在の通常ルールとの互換性を保ちます。

次は予告付き全体イベントの小さなcatalogと自動解決へ進み、その後に事前生成した地形を霧で隠すフロンティア探索、資源台帳とエスクローを基礎にした交易2.0を実装する想定です。詳細と安全上の境界は [`docs/variants-roadmap.md`](docs/variants-roadmap.md) を参照してください。
